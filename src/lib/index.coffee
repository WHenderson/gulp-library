module.exports = {
  gulp: require('gulp')
  metadata: require('./metadata')
  fs: require('./fs')
  pipe: require('./pipe')
  lint: require('./lint')
  transform: require('./transform')
  util: require('./util')
}